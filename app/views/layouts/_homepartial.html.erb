"<% if logged_in? %>
    <p>This works</p>
<% else %>
<div class="row">
    <div class="large-2 columns">
        <h2 style="text-align: center">  Get a Gee~Lib account<br><%= link_to "Sign Up", signup_url %></h2>
        <div>
        <div class="large-2 columns">
        
Already have an account? <%=link_to "LogIn Here", login_path%></h3>
        </div>
        </div>
        <% end %>

